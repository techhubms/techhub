@page "/admin/login"
@layout AdminLayout

@* This page exists as a direct URL for the login form.
   Since AdminLayout shows the login form inline via AuthorizeView when not authenticated,
   this page simply renders AdminLoginContent for direct /admin/login navigation. *@

<AdminLoginContent />
