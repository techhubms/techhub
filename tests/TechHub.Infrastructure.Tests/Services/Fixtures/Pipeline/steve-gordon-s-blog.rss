<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:wfw="http://wellformedweb.org/CommentAPI/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:sy="http://purl.org/rss/1.0/modules/syndication/" xmlns:slash="http://purl.org/rss/1.0/modules/slash/"><channel><title>Steve Gordon – Code with Steve</title><atom:link href="https://www.stevejgordon.co.uk/feed" rel="self" type="application/rss+xml" /><link>https://www.stevejgordon.co.uk/</link><description>Adventures in .NET</description><lastBuildDate>Thu, 05 Feb 2026 15:15:18 +0000</lastBuildDate><language>en-GB</language><sy:updatePeriod>
	hourly	</sy:updatePeriod><sy:updateFrequency>
	1	</sy:updateFrequency><generator>https://wordpress.org/?v=6.7.5</generator><image><url>https://www.stevejgordon.co.uk/wp-content/uploads/2019/07/cropped-Portrait-Small-32x32.png</url><title>Steve Gordon – Code with Steve</title><link>https://www.stevejgordon.co.uk/</link><width>32</width><height>32</height></image><item><title>Encrypting Properties with System.Text.Json and a TypeInfoResolver Modifier (Part 2)</title><link>https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-2</link><dc:creator><![CDATA[Steve Gordon]]></dc:creator><pubDate>Thu, 05 Feb 2026 15:15:17 +0000</pubDate><category><![CDATA[.NET]]></category><category><![CDATA[encryption]]></category><category><![CDATA[json]]></category><category><![CDATA[System.Text.Json]]></category><category><![CDATA[typeinforesolver]]></category><guid isPermaLink="false">https://www.stevejgordon.co.uk/?p=3213</guid><description><![CDATA[<p>In the previous post, we discussed the foundations for automatic data encryption of properties during serialisation using System.Text.Json. In this post, we&#8217;ll continue our journey, moving toward removing our fake &#8220;encryption&#8221; and replacing it with actual encryption. Along the way we&#8217;ll establish some abstractions and a design that allows us to develop flexible encryption and [&#8230;]</p>
<p>The post <a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-2">Encrypting Properties with System.Text.Json and a TypeInfoResolver Modifier (Part 2)</a> appeared first on <a href="https://www.stevejgordon.co.uk">Steve Gordon - Code with Steve</a>.</p>
<hr style="border-top: black solid 1px" /><a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-2">Encrypting Properties with System.Text.Json and a TypeInfoResolver Modifier (Part 2)</a> was first posted on February 5, 2026 at 3:15 pm.<br />&copy;2017 &quot;<a href="https://www.stevejgordon.co.uk">Steve Gordon&#039;s Blog</a>&quot;. Use of this feed is for personal non-commercial use only. If you are not reading this article in your feed reader, then the site is guilty of copyright infringement. Please contact me at <!--email_off-->sgordon@hotmail.co.uk<!--/email_off--><br />]]></description><content:encoded><![CDATA[
<div>In the <a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-1">previous post</a>, we discussed the foundations for automatic data encryption of properties during serialisation using System.Text.Json. In this post, we&#8217;ll continue our journey, moving toward removing our fake &#8220;encryption&#8221; and replacing it with actual encryption. Along the way we&#8217;ll establish some abstractions and a design that allows us to develop flexible encryption and decryption code. Originally, I expected this series to be just two parts, but it has become clear that I need to break things down a bit further as I expected my example.</div>
<div> </div>
<div>Here is the planned architecture that we&#8217;ll get to in the next few posts. We&#8217;ll also create other implementations (not shown) of KeyStore once this design is achieved. In <a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-1">part 1</a>, we completed the first version of the <code>EncryptedJsonTypeInfoResolverModifier</code>.</div>


<div class="wp-block-image">
<figure class="aligncenter size-full"><img fetchpriority="high" decoding="async" width="665" height="715" src="https://www.stevejgordon.co.uk/wp-content/uploads/2026/02/json-encryption-architecture.png" alt="A high-level architectural diagram of the types we'll be creating for JSON encryption, with the EncryptedJsonTypeInfoResolverModifier at the top, which depends on the DataProtector, which depends on the KeyProvider and AesGcmDataEncryptor." class="wp-image-3214" srcset="https://www.stevejgordon.co.uk/wp-content/uploads/2026/02/json-encryption-architecture.png 665w, https://www.stevejgordon.co.uk/wp-content/uploads/2026/02/json-encryption-architecture-279x300.png 279w" sizes="(max-width: 665px) 100vw, 665px" /></figure></div>


<div>Today, we&#8217;re going to build out the <code>AesGcmDataEncryptor</code> (in green), which can encrypt data using the AES-GCM standard. It&#8217;ll mostly be a wrapper around the built-in .NET cryptography API.</div>
<div> </div>
<div><strong><em>I want to warn you, once again, that while this is reasonably fully featured, it is not production-ready code and is still demoware. I haven&#8217;t thoroughly tested it, so it&#8217;s by no means battle-hardened. Use with caution!</em></strong></div>
<div> </div>
<div>Let&#8217;s start with the basic shape of this class.</div>


<div class="wp-block-syntaxhighlighter-code "><pre class="brush: csharp; title: ; notranslate">
using System.Buffers;
using System.Diagnostics.CodeAnalysis;
using System.Security.Cryptography;
using System.Text;

namespace JsonEncryptionBlogExample;

public sealed class AesGcmDataEncryptor
{
    public const int NonceSize = 12; // 96-bit
    public const int TagSize = 16; // 128-bit
    public const int Aes128KeySize = 16; // 128-bit
    public const int Aes256KeySize = 32; // 256-bit

    public bool TryEncrypt(ReadOnlySpan&lt;char&gt; plainText, ReadOnlySpan&lt;byte&gt; aesKey,
        Span&lt;byte&gt; cipherTextDestination, out int bytesWritten)
    {
        // TODO
    }

    public bool TryDecrypt(ReadOnlySpan&lt;byte&gt; cipherTextBytes, ReadOnlySpan&lt;byte&gt; aesKey,
        &#x5B;NotNullWhen(true)] out string? plainText)
    {
        // TODO
    }

    public static (int PlainTextLength, int CipherTextLength) CalculateByteLengths(ReadOnlySpan&lt;char&gt; plainText)
    {
        if (plainText.Length == 0)
            return (0, 0);

        var plainTextByteLength = Encoding.UTF8.GetByteCount(plainText);

        return (plainTextByteLength, NonceSize + plainTextByteLength + TagSize);
    }

    private static bool IsValidKeySize(int keySize) =&gt;
        keySize == Aes128KeySize || keySize == Aes256KeySize;
}
</pre></div>


<div>First, we define some constants for the lengths of the encryption inputs. The nonce and tag are core concepts in AES-GCM to provide a combination of security and integrity for the encryption process. We&#8217;re using the recommended sizes here. Our code will also support both 128-bit and 256-bit keys, which will be provided by the consumer of the code.</div>
<div> </div>
<div>There are two scaffolded methods that we&#8217;ll complete shortly that provide the encryption and decryption functionality. <code>TryEncrypt</code> accepts a <code>ReadOnlySpan&lt;char&gt;</code>, which includes directly passing in a string instance, containing the plain text. It then requires a <code>ReadOnlySpan&lt;byte&gt;</code> containing the key to be used for encryption. We&#8217;ll later implement a concept of a <code>KeyStore</code>, which will provide these keys. The method is designed for low allocation use, so it also requires a <code>Span&lt;byte&gt;</code> as the destination for the ciphertext that will be produced. Its out parameter returns the number of bytes written.</div>
<div> </div>
<div><code>TryDecrypt</code> has a very similar signature, but in reverse. The first parameter is <code>ReadOnlyString&lt;byte&gt;</code> representing the ciphertext bytes to be decrypted. The next <code>ReadOnlySpan&lt;byte&gt;</code> parameter is the AES key, and the out value is the plain text string.</div>
<div> </div>
<div>There are three static helper methods involved in calculating the byte lengths required for plain text and ciphertext. When using AES GCM, our final bytes include more than just our encrypted data. We also store the nonce and tag value in the final payload; therefore, we account for those additional bytes. Because we&#8217;re designing low-allocation code and using <code>Span&lt;T&gt;</code> for most of our data, we need to calculate the sizes upfront in order to rent appropriately sized arrays.</div>
<div> </div>
<div>Next, we&#8217;ll implement the <code>TryEncrypt</code> method.</div>


<div class="wp-block-syntaxhighlighter-code "><pre class="brush: csharp; title: ; notranslate">
public bool TryEncrypt(ReadOnlySpan&lt;char&gt; plainText, ReadOnlySpan&lt;byte&gt; aesKey,
    Span&lt;byte&gt; cipherTextDestination, out int bytesWritten)
{
    bytesWritten = 0;

    if (plainText.Length == 0)
        return true;

    if (!IsValidKeySize(aesKey.Length))
        return false;

    var (plainTextByteLength, cipherTextByteLength) = CalculateByteLengths(plainText);

    if (cipherTextDestination.Length &lt; cipherTextByteLength)
        return false;

    var rentedPlainTextArray = ArrayPool&lt;byte&gt;.Shared.Rent(plainTextByteLength);

    try
    {
        var plainTextBytes = rentedPlainTextArray.AsSpan();

        if (!Encoding.UTF8.TryGetBytes(plainText, plainTextBytes, out var plainTextBytesWritten))
            return false;

        using var aesGcm = new AesGcm(aesKey, TagSize);

        Span&lt;byte&gt; nonceAndTagBuffer = stackalloc byte&#x5B;NonceSize + TagSize];
        var nonceSpan = nonceAndTagBuffer&#x5B;..NonceSize];
        var tagSpan = nonceAndTagBuffer&#x5B;NonceSize..];

        RandomNumberGenerator.Fill(nonceSpan);

        aesGcm.Encrypt(nonceSpan, plainTextBytes&#x5B;..plainTextBytesWritten],
            cipherTextDestination.Slice(NonceSize, plainTextBytesWritten), tagSpan);

        nonceSpan.CopyTo(cipherTextDestination);
        tagSpan.CopyTo(cipherTextDestination.Slice(NonceSize + plainTextBytesWritten, TagSize));

        bytesWritten = NonceSize + plainTextBytesWritten + TagSize;
        return true;
    }
    finally
    {
        ArrayPool&lt;byte&gt;.Shared.Return(rentedPlainTextArray, clearArray: true);
    }
}
</pre></div>


<div>Upfront, there is some validation being done. If the input plain text bytes are 0, we have nothing to do. If the provided key length is not valid for 128-bit or 256-bit encryption, we return false. Next, we calculate the required size in bytes of both the plain text (characters at this stage) and the final ciphertext. If the destination <code>Span&lt;byte&gt;</code> we have been provided for the final ciphertext (including the nonce and tags) is too small, we also return false because we can&#8217;t encrypt into an undersized buffer.</div>
<div> </div>
<div>With the validation complete, the first job is to encode the plain text characters into UTF8 bytes. The code first rents a suitably sized byte array and then creates a <code>Span&lt;byte&gt;</code> over the memory. We can then use the <code>TryGetBytes</code> method on <code>UTF8</code> to encode the plain text characters from the original string value into the byte array.</div>
<div> </div>
<div>We can now initialise an <code>AesGcm</code> instance with our key and our required tag size. The computed tag will later be used for integrity validation. When we later encrypt our data, we&#8217;ll need to pass in a <code>ReadOnlySpan&lt;byte&gt;</code> containing the nonce value and a <code>Span&lt;byte&gt;</code> large enough for the computed tag value. This non-sensitive data is only a few bytes, so the code stack allocates the appropriate number of bytes. This is a very efficient way to handle small, temporary memory, and it&#8217;s made safe through explicitly typing the result as <code>Span&lt;byte&gt;</code>.</div>
<div> </div>
<div>We slice the stack-allocated memory to get our nonce span and our tag span. For the nonce, we need to provide a unique value per encryption operation. In this code, we use the <code>RandomNumberGenerator</code> to fill it. For this use case and example, this is sufficient. There is, over time, a very small chance of generating the same nonce, which can render AES-GCM insecure. You&#8217;d need to perform billions of encryptions before that risk becomes a real concern. A richer implementation could use an incrementing value to avoid the risk of repeating the same nonce with the same key, but it&#8217;s likely overkill for this scenario. We could also rotate keys periodically to avoid the risk of the same key and nonce being used.</div>
<div> </div>
<div>We now have all the pieces in place and can call Encrypt on our <code>AesGcm</code> instance. We pass the span of bytes containing our nonce value, the plain text bytes, the destination <code>Span&lt;byte&gt;</code> slices to the correct size and the span of bytes for the tag to be written into. Note that with AES-GCM, we know the number of bytes required for the ciphertext will match the number of bytes being encrypted. The slice operation slices the destination starting from the index matching the nonce size, as we will later write the nonce bytes into the destination span, since it is also required for decryption. In fact, the next line of code copies the nonce bytes into the start of the destination span.</div>
<div> </div>
<div>The tag span will also have been populated with the value used to verify the integrity of the encrypted data, so we also copy this to the end of the destination span.</div>
<div> </div>
<div>We then set the bytesWritten out variable with the actual bytes written to the cipherTextDestination span and return true. Inside the finally block, the rented array used for the plain text bytes is returned to the pool. Crucially, we specifically pass true for the second argument so that it is cleared first. We don&#8217;t want future renters having access to the plain text data.</div>
<div> </div>
<div>We must also implement the <code>TryDecrypt</code> method:</div>


<div class="wp-block-syntaxhighlighter-code "><pre class="brush: csharp; title: ; notranslate">
public bool TryDecrypt(ReadOnlySpan&lt;byte&gt; cipherTextBytes, ReadOnlySpan&lt;byte&gt; aesKey,
    &#x5B;NotNullWhen(true)] out string? plainText)
{
    plainText = null;

    if (cipherTextBytes.Length &lt; NonceSize + TagSize || !IsValidKeySize(aesKey.Length))
        return false;

    var plainTextLength = cipherTextBytes.Length - NonceSize - TagSize;

    var plainTextLength = cipherTextBytes.Length - NonceSize - TagSize;
    var rentedPlainTextArray = ArrayPool&lt;byte&gt;.Shared.Rent(plainTextLength);
    var plainTextBytes = rentedPlainTextArray.AsSpan(0, plainTextLength);

    try
    {
        var nonce = cipherTextBytes&#x5B;..NonceSize];
        var encryptedPayload = cipherTextBytes.Slice(NonceSize, plainTextLength);
        var tag = cipherTextBytes.Slice(NonceSize + plainTextLength, TagSize);

        using var aesGcm = new AesGcm(aesKey, TagSize);
        aesGcm.Decrypt(nonce, encryptedPayload, tag, plainTextBytes);

        plainText = Encoding.UTF8.GetString(plainTextBytes);
        return true;
    }
    catch
    {
        return false;
    }
}
</pre></div>


<div>Again, there is some validation up front to check that the ciphertext bytes are at least long enough to hold the nonce and tag and that the provided key is a valid length.</div>
<div> </div>
<div>We can then calculate the plain text byte length by subtracting the nonce size and tag size and rent an appropriately sized array, before creating a span over the bytes we will populate.</div>
<div> </div>
<div>We extract the spans for the nonce, payload and tag by slicing the incoming cipherTextBytes. We can initialise a new <code>AesGcm</code> instance with the key and the tag size before calling <code>Decrypt</code>, passing the nonce span, the encrypted payload, tag and the <code>Span&lt;byte&gt;</code> for the plain text to be decrypted into.</div>
<div> </div>
<div>We ultimately require a string for the decrypted data, so we can use <code>UTF8.GetString</code>, passing in the plain text bytes from our decryption operation. When everything goes to plan, we return true from this method; otherwise, any exceptions will cause us to return false.</div>
<div> </div>
<div>And that&#8217;s it! The encryption process is quite simple. It&#8217;s the use of low-allocation spans that makes it a little more complex, but even so, you now hopefully understand what&#8217;s happening at this layer. I&#8217;ve used low-allocation code here because I know this will be on hot paths for my authentication flow, and I want to avoid heavy overhead for each request in the application where I will eventually use this code.</div>
<div> </div>
<div>As this part ended up being quite long, we&#8217;ll leave it there, and in the next part, we can focus on the <code>KeyProvider</code> abstraction.</div>
<h3><span style="color: revert; font-size: revert; font-weight: revert;">Other posts in this series:</span></h3>
<div>
<ul>
<li><a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-1">Part One</a></li>
<li>Part Two &#8211; This post!</li>
</ul>
</div>


<p>The post <a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-2">Encrypting Properties with System.Text.Json and a TypeInfoResolver Modifier (Part 2)</a> appeared first on <a href="https://www.stevejgordon.co.uk">Steve Gordon - Code with Steve</a>.</p>
<hr style="border-top: black solid 1px" /><a href="https://www.stevejgordon.co.uk/encrypting-properties-with-system-text-json-and-a-typeinforesolver-modifier-part-2">Encrypting Properties with System.Text.Json and a TypeInfoResolver Modifier (Part 2)</a> was first posted on February 5, 2026 at 3:15 pm.<br />&copy;2017 &quot;<a href="https://www.stevejgordon.co.uk">Steve Gordon&#039;s Blog</a>&quot;. Use of this feed is for personal non-commercial use only. If you are not reading this article in your feed reader, then the site is guilty of copyright infringement. Please contact me at <!--email_off-->sgordon@hotmail.co.uk<!--/email_off--><br />]]></content:encoded></item></channel></rss>
